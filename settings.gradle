include 'jsf-core'
include 'jsf-webflux'
include 'jsf-graphql'
include 'jsf-graphql-webflux'

rootProject.name = "jsf"
rootProject.children.forEach { project ->
  project.buildFileName = "${project.name}.gradle"
}

